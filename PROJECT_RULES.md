# TidyCSV Project Rules

## Project

TidyCSV

## Project Purpose

TidyCSV는 첫 번째 학습용 소프트웨어 개발 프로젝트다.

가장 중요한 목표는 프로그램을 빠르게 완성하는 것이 아니다.

**Codex를 활용하면서 실제 개발 실력을 쌓는 것이 최우선 목표다.**

최종적으로 사용자는 다음 능력을 갖추는 것을 목표로 한다.

- 코드를 읽고 대략적인 역할을 이해한다.
- 원본 데이터와 작업용 데이터를 구분한다.
- 작은 기능을 직접 수정할 수 있다.
- 오류 원인을 이해하고 검증할 수 있다.
- Git과 GitHub를 이용해 프로젝트를 관리한다.
- AI가 작성한 코드를 무조건 신뢰하지 않고 직접 테스트한다.
- 문제를 작은 개발 작업으로 나눌 수 있다.
- 프로그램을 기획 → 개발 → 테스트 → 배포할 수 있다.

## Development Rules

1. 한 번에 기능 하나만 구현한다.
2. 기존 기능을 깨뜨리지 않는다.
3. 요청하지 않은 기능을 임의로 추가하지 않는다.
4. 대규모 리팩터링은 사용자의 요청 없이 하지 않는다.
5. 기능 구현 후 반드시 직접 검증한다.
6. 사용자가 직접 실행하기 전에는 기능 완료로 간주하지 않는다.
7. 원본 DataFrame은 가능하면 보존한다.
8. 데이터 정리는 작업용 복사본에서 수행한다.
9. 기존 기능에 영향을 주는지 회귀 검증한다.
10. Git Commit 전에 변경 파일을 반드시 확인한다.
11. 테스트용 개인 CSV를 임의로 Git에 추가하지 않는다.
12. `.venv`는 Git에 추가하지 않는다.
13. 사용자의 파일을 임의로 삭제하거나 수정하지 않는다.
14. Git 충돌 가능성이 있으면 자동 해결하지 말고 먼저 보고한다.
15. 기능 하나가 검증된 뒤에 다음 기능으로 넘어간다.

## Learning Rules

기능 구현 후 반드시 다음 학습 단계를 제공한다.

### 1. 핵심 코드

이번 기능과 직접 관계있는 코드만 5~10줄 정도 보여준다.

### 2. 초보자 설명

코드가 무슨 일을 하는지 쉬운 한국어로 설명한다.

문법 자체보다 다음 내용을 중심으로 설명한다.

- 어떤 데이터인가?
- 어떤 기능인가?
- 결과가 어디에 저장되는가?

### 3. 오늘의 개념

한 번에 핵심 개념 2~4개만 알려준다.

모든 문법을 외우게 하지 않는다.

### 4. 데이터 흐름

입력부터 결과까지 데이터가 이동하는 과정을 설명한다.

### 5. 직접 수정 과제

사용자가 VS Code에서 직접 변경할 수 있는 매우 작은 코드 수정 과제를 1개 준다.

예:

- 화면 문구 수정
- 숫자 변경
- 간단한 옵션 변경

### 6. 이해도 퀴즈

현재 기능과 관련된 초급 질문 3개를 낸다.

목적은 문법 암기가 아니라 개념 이해 확인이다.

## Current Learning Philosophy

사용자는 완전한 개발 초보자다.

따라서 설명은 최대한 쉽게 한다.

하지만 Codex가 모든 것을 대신하도록 만들지 않는다.

기본 개발 사이클은 다음과 같다.

구현
→ 직접 실행
→ 핵심 코드 확인
→ 개념 이해
→ 직접 작은 수정
→ 퀴즈
→ Commit
→ Push

## Git Workflow

### 작업 시작

1. `git status`
2. 변경사항이 없다면 `git pull`
3. `PROJECT_RULES.md` 읽기
4. `DEV_LOG.md` 읽기
5. 현재 작업 확인

Commit되지 않은 변경사항이 있으면 임의로 pull, stash, reset, commit 하지 않고 먼저 사용자에게 보고한다.

기존 파일을 잃을 가능성이 있는 Git 작업은 절대 임의로 하지 않는다.

### 기능 개발 완료

1. 직접 실행
2. 기능 검증
3. 변경 파일 확인
4. 필요한 파일만 `git add`
5. Commit
6. Push
7. `DEV_LOG.md` 업데이트

`git add .`은 사용자 파일까지 포함할 가능성이 있으므로 상황을 확인하지 않고 사용하지 않는다.
